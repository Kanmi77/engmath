# EngMath: Engineering Mathematics Toolkit

**EngMath** is a modular Python library for complex mathematical and engineering operations. It includes tools for algebra, linear algebra, differential equations, signal processing, and numerical analysis — all wrapped in clean, object-oriented design.

---

## 📦 Module Structure

- `algebra.py` — Complex number operations with operator overloading
- `linear.py` — Matrix operations: determinant, inverse, eigenvalues, linear systems
- `dynamics.py` — Solvers for single and system ODEs (Euler, Runge-Kutta)
- `utils.py` — Helper functions: interpolation, inverse Z-transform, numerical integration

---

## 🚀 Installation

Clone the repo and install dependencies:

```bash
git clone https://github.com/yourusername/engmath.git
cd engmath
pip install -r requirements.txt